# 服务拓扑说明

## 功能概述

服务拓扑页提供**跳转链接**，在新窗口打开 Kiali 查看服务依赖关系、流量走向与健康状态。

## 配置

设置环境变量 `KIALI_URL` 为浏览器可访问的 Kiali 地址，例如：

```bash
export KIALI_URL=http://47.121.202.218:20001
```

未配置时，拓扑页会提示「未配置 Kiali」。

## 使用说明

1. 登录 Dashboard 后，点击侧边栏「服务拓扑」
2. 点击「打开 Kiali」按钮，在新窗口打开 Kiali
